
# Import parameters
{% import_yaml 'sysctl/sysctl.yaml' as defaults with context %}

# merge the pillar
{% set sysctl = salt['grains.filter_by']({
    'Linux': defaults
}, grain='kernel', merge=salt['pillar.get']('scality:sysctl'))%}
