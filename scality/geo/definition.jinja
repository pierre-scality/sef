#import parameters
{% import_yaml 'scality/geo/definition.yaml' as defaults with context %}

# merge the pillar
{% set definition = salt['grains.filter_by']({
    'Linux': defaults
}, grain='kernel', merge=salt['pillar.get']('definition:lookup'))%}

