
# Import parameters
{% import_yaml 'scalitycloud/pkg.yaml' as defaults with context %}

# merge the pillar
{% set pkglist = salt['grains.filter_by']({
    'Linux': defaults
}, grain='kernel', merge=salt['pillar.get']('pkglist'))%}
