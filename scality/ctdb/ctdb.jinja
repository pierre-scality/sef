#import parameters
{% import_yaml 'scality/ctdb/ctdb.yaml' as defaults with context %}

# merge the pillar
{% set ctdb = salt['grains.filter_by']({
    'Linux': defaults
}, grain='kernel', merge=salt['pillar.get']('ctdb:lookup'))%}
