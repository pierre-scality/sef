#import parameters
{% import_yaml 'scality/corosync/corosync.yaml' as defaults with context %}

# merge the pillar
{% set corosync = salt['grains.filter_by']({
    'Linux': defaults
}, grain='kernel', merge=salt['pillar.get']('corosync:lookup'))%}
