#import parameters
{% import_yaml 'scality/samba/samba.yaml' as defaults with context %}

# merge the pillar
{% set samba = salt['grains.filter_by']({
    'Linux': defaults
}, grain='kernel', merge=salt['pillar.get']('samba:lookup'))%}
